# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# #---Starting External Scripts---# #

# --------------------- UTILITIES ---------------------
# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 7 px or 7 ppt
        bindsym k resize grow height 7 px or 7 ppt
        bindsym l resize shrink height 7 px or 7 ppt
        bindsym semicolon resize grow width 7 px or 7 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 7 px or 7 ppt
        bindsym Up resize grow height 7 px or 7 ppt
        bindsym Down resize shrink height 7 px or 7 ppt
        bindsym Right resize grow width 7 px or 7 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# --------------------- STARTUP ---------------------
exec_always --no-startup-id feh --bg-scale ~/Pictures/wall.jpg

# --------------------- SETTINGS ---------------------
set $mod Mod4
set $mod1 Mod1
focus_follows_mouse no
focus_wrapping no

# --------------------- CUSTOM CONTROLS ---------------------
bindsym $mod+n exec --no-startup-id amixer -D pulse sset Master 5%-
bindsym $mod+m exec --no-startup-id amixer -D pulse sset Master 5%+
bindsym $mod+b exec --no-startup-id amixer -D pulse set Master 1+ toggle
bindsym Pause exec --no-startup-id ~/lock.sh
bindsym $mod+l exec --no-startup-id ~/lock.sh
bindsym $mod+e exec --no-startup-id nautilus
#bindsym $mod+Return exec zsh
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec konsole
#bindsym $mod+Return exec /bin/bash
bindsym $mod+q kill
bindsym $mod+space exec --no-startup-id i3-dmenu-desktop
bindsym $mod+Shift+u reload
bindsym $mod+Shift+i restart
bindsym $mod+Shift+q exec "i3-nagbar -t warning -m 'Do you want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# --------------------- MACOS LIKE BUTTONS ---------------------
bindsym --release $mod+c exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+c
bindsym --release $mod+v exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+v
bindsym --release $mod+Shift+c exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+Shift+c
bindsym --release $mod+Shift+v exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+Shift+v
bindsym --release $mod+a exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+a
bindsym --release $mod+y exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+y
bindsym --release $mod+z exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+z
bindsym --release $mod+w exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+w
bindsym --release $mod+r exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+r
bindsym --release $mod+f exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+f
bindsym --release $mod+t exec --no-startup-id /home/fractalman/xdotool_hack.sh key --clearmodifiers ctrl+t

# --------------------- APPEARANCE ---------------------
# Take out window borders
new_window pixel 4
hide_edge_borders horizontal
for_window [class="^.*"] border pixel 3

# class                 border  backgr. text    indicator
client.focused          #E800C7  #A800C7 #E800C7 #E800C7
#client.focused_inactive #333333 #5f676a #ffffff #484e50
#client.unfocused        #333333 #222222 #888888 #292d2e
#client.urgent           #2f343a #900000 #ffffff #900000
#client.placeholder      #000000 #0c0c0c #ffffff #000000

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10
# font pango: Monospace 8

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# --------------------- NAVIGATION ---------------------
# change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+d split h
# split in vertical orientation
bindsym $mod+Shift+s split v

# enter fullscreen mode for the focused container
bindsym $mod+Shift+a fullscreen
# change container layout (toggle split)
bindsym $mod+Shift+r layout toggle split
# toggle tiling / floating
bindsym $mod+Shift+w floating toggle

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

bindsym $mod+Shift+1 move container to workspace 1; workspace 1
bindsym $mod+Shift+2 move container to workspace 2; workspace 2
bindsym $mod+Shift+3 move container to workspace 3; workspace 3
bindsym $mod+Shift+4 move container to workspace 4; workspace 4
bindsym $mod+Shift+5 move container to workspace 5; workspace 5
bindsym $mod+Shift+6 move container to workspace 6; workspace 6
bindsym $mod+Shift+7 move container to workspace 7; workspace 7
bindsym $mod+Shift+8 move container to workspace 8; workspace 8
bindsym $mod+Shift+9 move container to workspace 9; workspace 9
bindsym $mod+Shift+0 move container to workspace 10; workspace 10

# monitor
workspace 1 output DVI-D-0
workspace 2 output DVI-D-0
workspace 3 output DVI-D-0
workspace 4 output DVI-D-0
workspace 5 output DVI-D-0
workspace 6 output DVI-D-0
workspace 7 output DVI-D-0
workspace 8 output DVI-D-0
workspace 9 output DVI-D-0
workspace 10 output DVI-D-0

bindsym $mod+Shift+e mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    position top
    font pango:Monospace 10
    status_command i3status -c /home/fractalman/.config/i3status/i3status.conf
    tray_output DP-0
    colors {
        background #000000
        statusline #ffffff
        separator #ffa8a8
    }
}
